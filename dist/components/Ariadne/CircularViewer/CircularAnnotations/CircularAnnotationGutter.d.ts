import { AnnotatedSequence, StackedAnnotation } from '@Ariadne/types';

export declare const CircularAnnotationGutter: ({ stackedAnnotations, cx, cy, radius, annotatedSequence, }: {
    stackedAnnotations: StackedAnnotation[];
    cx: number;
    cy: number;
    radius: number;
    annotatedSequence: AnnotatedSequence;
}) => import("react/jsx-runtime").JSX.Element;
