import { AnnotatedSequence, Annotation, Coor } from '@Ariadne/types';

export declare const CircularAnnotation: ({ sequence, annotation, radius, center, }: {
    sequence: AnnotatedSequence;
    radius: number;
    annotation: Annotation;
    center: Coor;
}) => import("react/jsx-runtime").JSX.Element;
